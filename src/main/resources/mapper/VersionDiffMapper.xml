<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.docversion.mapper.VersionDiffMapper">

    <!-- onDocumentModified 후처리: diff 캐시 적재.
         INSERT IGNORE — UNIQUE(file_id,from,to) 충돌 시 무시 (MariaDB 확정 전제). -->
    <insert id="insertIgnore">
        INSERT IGNORE INTO version_diffs
            (file_id, from_version_id, to_version_id, diff_method,
             added_lines, deleted_lines, summary, hunks_json, created_at)
        VALUES
            (#{fileId}, #{fromVersionId}, #{toVersionId}, #{diffMethod},
             #{addedLines}, #{deletedLines}, #{summary}, #{hunksJson}, #{createdAt})
    </insert>

    <!-- UI 직접 조회: 캐시 단건 -->
    <select id="findCached" resultType="map">
        SELECT diff_method  AS diffMethod,
               added_lines  AS addedLines,
               deleted_lines AS deletedLines,
               summary,
               hunks_json   AS hunksJson,
               created_at   AS createdAt
        FROM version_diffs
        WHERE file_id = #{fileId}
          AND from_version_id = #{fromVersionId}
          AND to_version_id = #{toVersionId}
        LIMIT 1
    </select>

</mapper>
